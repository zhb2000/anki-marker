/**
 * - `not-added`: 未添加到 Anki
 * - `processing-add`: 正在处理添加
 * - `processing-remove`: 正在处理删除
 * - `is-added`: 已添加到 Anki
 */
export type CardStatus = 'not-added' | 'processing-add' | 'processing-remove' | 'is-added';
